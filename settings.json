{
  "permissions": {
    "allow": [
      "Read",
      "Glob",
      "Grep",
      "Write(/pipeline/**)",
      "Edit(/pipeline/**)",
      "WebSearch",
      "WebFetch",
      "Bash(ls *)",
      "Bash(mkdir pipeline literature samples)",
      "Bash(cat *)"
    ]
  }
}
